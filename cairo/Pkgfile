# Description: A 2D graphics library with opengl support
# URL: http://www.cairographics.org/
# Maintainer: xcko, xcko at bitmessage dot ch
# Depends on: fontconfig, libpng, xorg-libxext, xorg-libxrender, xorg-libpixman, xorg-xcb-util, glu

name=cairo
version=1.15.4
release=1
source=(https://cairographics.org/snapshots/cairo-$version.tar.xz)

build() {
        cd $name-$version
        ./configure --prefix=/usr \
                --enable-xcb=yes \
                --enable-ps \
                --enable-pdf \
		--enable-gl \
                --disable-gtk-doc \
                --enable-tee=yes
        make
        make DESTDIR=$PKG install
        rm -rf $PKG/usr/share
}
