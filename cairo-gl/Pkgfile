# Description: A 2D graphics library with opengl backend
# URL: http://www.cairographics.org/
# Maintainer: Fredrik Rinnestam, fredrik at crux dot nu
# Depends on: fontconfig libpng xorg-libxext xorg-libxrender xorg-libpixman xorg-xcb-util glib glu

name=cairo-gl
version=1.15.4
release=1
source=(https://cairographics.org/snapshots/cairo-$version.tar.xz)

build() {
        cd cairo-$version
        ./configure --prefix=/usr \
                --enable-xcb=yes \
                --enable-ps \
                --enable-pdf \
                --disable-gtk-doc \
                --enable-tee=yes \
		--enable-gl=yes
        make
        make DESTDIR=$PKG install
        rm -rf $PKG/usr/share
}
